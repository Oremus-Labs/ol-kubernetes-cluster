service:
  type: ClusterIP
  port: 80

ingress:
  enabled: false

auth:
  oauth:
    enabled: true
    provider: generic

config:
  oidc:
    secret:
      create: false
      name: headlamp-oidc
    externalSecret:
      enabled: true
      name: headlamp-oidc-env
