apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smb-data
  namespace: smb
  labels:
    app: smb-server
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 2Ti
  storageClassName: longhorn-smb-xfs
