apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "duckduckgo-mcp.fullname" . }}
  labels:
    {{- include "duckduckgo-mcp.labels" . | nindent 4 }}
data:
  server.py: |
{{ include "duckduckgo-mcp.serverPy" . | indent 4 }}
