apiVersion: v2
name: jenkins-wrapper
description: Wrapper chart that pins the upstream Jenkins Helm chart with Oremus Labs defaults
version: 0.1.0
type: application
dependencies:
  - name: jenkins
    version: 5.8.110
    repository: https://charts.jenkins.io
