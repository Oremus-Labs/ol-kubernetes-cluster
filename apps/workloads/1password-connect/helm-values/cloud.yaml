# 1Password Connect + Operator Helm values (cloud provider)
connect:
  enabled: true
  existingSecret: op-credentials
  service:
    type: ClusterIP
operator:
  create: true
  token:
    existingSecret: onepassword-connect-token
  autoRestart: false
