apiVersion: v1
kind: Service
metadata:
  name: {{ include "github-mcp-wrapper.fullname" . }}
  labels:
    {{- include "github-mcp-wrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "github-mcp-wrapper.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
