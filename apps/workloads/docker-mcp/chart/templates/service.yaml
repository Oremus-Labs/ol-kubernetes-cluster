apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker-mcp-wrapper.fullname" . }}
  labels:
    {{- include "docker-mcp-wrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "docker-mcp-wrapper.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
