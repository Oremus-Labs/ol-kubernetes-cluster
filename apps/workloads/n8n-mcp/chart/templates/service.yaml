apiVersion: v1
kind: Service
metadata:
  name: {{ include "n8n-mcp-wrapper.fullname" . }}
  labels:
    {{- include "n8n-mcp-wrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "n8n-mcp-wrapper.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
