apiVersion: v1
kind: Service
metadata:
  name: {{ include "bitbucket-mcp-wrapper.fullname" . }}
  labels:
    {{- include "bitbucket-mcp-wrapper.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bitbucket-mcp-wrapper.selectorLabels" . | nindent 4 }}
