namespace: ai

gatewayApi:
  hosts:
    - ai.oremuslabs.app
  parentRefs:
    - name: traefik-public
      namespace: traefik
      sectionName: websecure
  backendService:
    name: active-llm-predictor
    port: 80
  modelManagerService:
    # Route to the stable ClusterIP service that fronts the latest Knative revision
    name: model-manager-stable
    port: 80
