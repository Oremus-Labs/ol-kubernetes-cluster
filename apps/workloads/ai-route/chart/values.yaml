namespace: ai

gatewayApi:
  hosts:
    - ai.oremuslabs.app
  parentRefs:
    - name: traefik-public
      namespace: traefik
      sectionName: websecure
  backendService:
    name: active-llm-predictor
    port: 80
  modelManagerService:
    # Use the Knative revision private service which exposes port 80
    # (Traefik doesn't support ExternalName type services)
    name: model-manager-00001-private
    port: 80
